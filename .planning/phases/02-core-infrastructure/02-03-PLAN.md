---
phase: 02-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements:
  - CHAIN-01
  - CHAIN-03
  - CHAIN-04
  - CHAIN-05
  - PARAMS-04

must_haves:
  truths:
    - "processBlock never calls new / malloc / AudioBuffer::setSize on the host buffer or on any local variable"
    - "A pre-allocated stereo workBuffer exists as a member of PluginProcessor, sized in prepareToPlay"
    - "A pre-allocated preUndBuffer and preBurninBuffer exist as members (or are replaced with in-place bypass smoothing against workBuffer)"
    - "WR-01 (bypassRamp dead var), WR-02 (dcCoeff dead var), WR-04 (buffer.setSize), and WR-05 (unconditional persisted temp load) are fixed"
    - "Input limiter still applies soft tanh at unity at plugin boundary before Stage 1"
    - "DC-block filters (5 Hz HPF) still fire after each of the three stage outputs (mono post-RAT, stereo post-Pitch, stereo post-Und)"
    - "Per-stage bypass crossfade still operates with 10ms smoothing via juce::SmoothedValue"
    - "A sine input produces a sine output through the pass-through stage chain with zero DC accumulation and no clicks on bypass toggle"
  artifacts:
    - path: "Source/PluginProcessor.h"
      provides: "workBuffer, preUndBuffer, preBurninBuffer members; removed dcBlock1R (IN-02); updated members for WR-03 in Plan 02-04"
      contains: "workBuffer"
    - path: "Source/PluginProcessor.cpp"
      provides: "Fixed processBlock with pre-allocated buffers, no setSize, WR-01/02/04/05 resolved, IN-01 dead call removed"
      contains: "workBuffer"
  key_links:
    - from: "prepareToPlay"
      to: "workBuffer.setSize(2, samplesPerBlock, false, true, false)"
      via: "stereo pre-allocation"
      pattern: "workBuffer\\.setSize"
    - from: "processBlock (Stage 2 expansion)"
      to: "workBuffer (not the host buffer)"
      via: "mono→stereo copy into workBuffer, processing in workBuffer, copyFrom back to host buffer"
      pattern: "workBuffer"
    - from: "setStateInformation (WR-05 fix)"
      to: "pBurninPersist->load() > 0.5f gate"
      via: "conditional read of persistedTemp/persistedTempPrev"
      pattern: "persistEnabled"
---

<objective>
Rebuild the processBlock signal-chain skeleton to remove all real-time allocation,
fix the four outstanding WR findings that live in this file (WR-01, WR-02, WR-04,
WR-05), clean up the IN-01/IN-02 dead items, and switch the mono→stereo expansion
from a host-buffer setSize to a pre-allocated stereo workBuffer. This plan does not
touch oversampling or inter-stage trims or the feedback loop — those are Plan 02-04.

Purpose: CHAIN-01 (mono-sum input at Stage 1), CHAIN-03 (DC block after each stage),
CHAIN-04 (input limiter at plugin boundary), CHAIN-05 (per-stage bypass crossfade
and global wet/dry mix), and the no-alloc guarantee of BUILD-04 all depend on a
processBlock that allocates zero buffers, mutates zero host-owned memory, and uses
stable pre-sized member buffers for every working copy.

Output: A correct, allocation-free processBlock skeleton with all WR-01/02/04/05
fixes applied, all IN-01/02 cleanups done, passing a sine through all four
pass-through stages with no DC offset, no clicks, and no reallocation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-infrastructure/02-RESEARCH.md
@.planning/phases/01-project-scaffold/01-REVIEW.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@cpp files for agent/PluginProcessor.h
@cpp files for agent/PluginProcessor.cpp
@Source/dsp/TurboRat.h
@Source/dsp/MicroPitch.h
@Source/dsp/Undulator.h
@Source/dsp/BurnIn.h

<interfaces>
<!-- Target structure for this plan. Everything in Phase 2 lives inside
     PluginProcessor; DSP stages remain pass-through stubs. -->

New / changed private members in PluginProcessor.h:

  // WR-04: Pre-allocated stereo working buffer for mono→stereo expansion.
  // Sized to max block size in prepareToPlay. Never resized in processBlock.
  juce::AudioBuffer<float> workBuffer;

  // Pre-allocated buffers for bypass crossfade snapshots. Phase 1 allocated
  // these inline inside processBlock (AudioBuffer ctor on stack = heap alloc).
  juce::AudioBuffer<float> preUndBuffer;
  juce::AudioBuffer<float> preBurninBuffer;

  // IN-02: dcBlock1R removed (mono stage — right channel never exists).
  // dcBlock1L remains for post-RAT mono DC block.
  // dcBlock2L/R and dcBlock3L/R remain for post-Pitch and post-Und stereo DC block.

Signal-chain order inside processBlock (unchanged from Phase 1 other than buffer
discipline + WR fixes):

  0. ScopedNoDenormals
  1. updateTimerState(numSamples)
  2. dryBuffer.copyFrom(0, 0, buffer, 0, 0, numSamples)     // mono dry snapshot
  3. [feedback injection — owned by Plan 02-04, leave existing code in place for now]
  4. inputLimiter.process(mono AudioBlock)                   // soft tanh at unity
  5. Stage 1 (RAT) — mono process on buffer channel 0 (pass-through stub)
  6. Bypass crossfade mono (bypassSmoothRat) against dryBuffer
  7. dcBlock1L.processSample per mono sample
  8. [trim post-RAT — owned by Plan 02-04]
  9. Stage 2 (MicroPitch) — WR-04 FIX:
       - Copy mono buffer channel 0 into workBuffer.channel(0)
       - Copy mono buffer channel 0 into workBuffer.channel(1)
       - Pass workBuffer to stageMicroPitch->process(workBuffer, numSamples)
       - DO NOT call buffer.setSize(2, ...)
 10. Bypass crossfade stereo (bypassSmoothPitch) inside workBuffer, against
     expanded dryBuffer (mono dry used for both L and R)
 11. dcBlock2L/R stereo DC block on workBuffer
 12. [trim post-Pitch — Plan 02-04]
 13. Stage 3 (Undulator):
       - preUndBuffer.copyFrom both channels from workBuffer (uses pre-allocated
         member — no AudioBuffer ctor inside processBlock)
       - stageUndulator->process(workBuffer, numSamples)
 14. Bypass crossfade stereo (bypassSmoothUnd) using preUndBuffer as dry source
 15. dcBlock3L/R stereo DC block on workBuffer
 16. [trim post-Und — Plan 02-04]
 17. Stage 4 (BurnIn):
       - preBurninBuffer.copyFrom both channels from workBuffer
       - stageBurnIn->process(workBuffer, numSamples)
 18. Bypass crossfade stereo (bypassSmoothBurnin) using preBurninBuffer as dry source
 19. Copy workBuffer → host `buffer` stereo channels  (host buffer is already
     stereo from the output bus, so this is a pair of copyFrom calls)
 20. Global wet/dry blend on host buffer (globalMixSmooth) against mono dryBuffer
 21. [feedback update for next block — Plan 02-04]
 22. Output safety clip (isfinite + jlimit ±2.0) across both channels

DC-block filter member changes:
  - Remove dcBlock1R declaration and its prepare()/reset() calls
  - Keep dcBlock1L (post-RAT mono)
  - Keep dcBlock2L, dcBlock2R (post-Pitch stereo)
  - Keep dcBlock3L, dcBlock3R (post-Und stereo)

WR fixes owned by this plan:
  - WR-01: Delete `bypassRamp` dead variable (lines ~93-94 of current Phase 1 file)
  - WR-02: Delete `dcCoeff` dead variable (lines ~72-73)
  - WR-04: Replace buffer.setSize(2, ...) pattern with workBuffer member as above
  - WR-05: Gate persistedTemp / persistedTempPrev restoration on pBurninPersist load
  - IN-01: Remove the `stageMicroPitch->getLastLfoValue();` dead call; replace with
           a `// Phase 4: cross-feed from Undulator LFO into MicroPitch will be
           // wired inside Undulator::process` comment
  - IN-02: Remove dcBlock1R member (dead — mono stage has no right channel)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pre-allocated member buffers and fix WR-01, WR-02, IN-02 in PluginProcessor.h / prepareToPlay / releaseResources</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <action>
In Source/PluginProcessor.h, private section:

1. Add three new members alongside the existing `juce::AudioBuffer<float> dryBuffer;`:

       juce::AudioBuffer<float> workBuffer;       // WR-04: stereo mono→stereo expansion target
       juce::AudioBuffer<float> preUndBuffer;     // bypass crossfade snapshot (pre-Undulator)
       juce::AudioBuffer<float> preBurninBuffer;  // bypass crossfade snapshot (pre-BurnIn)

2. Delete `dcBlock1R` from the declaration line:
       juce::dsp::IIR::Filter<float> dcBlock1L, dcBlock1R;   // post-RAT
   becomes:
       juce::dsp::IIR::Filter<float> dcBlock1L;              // post-RAT (mono)

   Leave dcBlock2L/R and dcBlock3L/R unchanged.

In Source/PluginProcessor.cpp::prepareToPlay:

3. Delete WR-02 dead variable at line ~72-73:
       const float dcCoeff = 1.0f - (juce::MathConstants<float>::twoPi * 5.0f
                                     / static_cast<float>(sampleRate));
   Just remove both lines. The `dcCoeffs` obtained from `makeHighPass(sampleRate, 5.0f)`
   is the real coefficient source.

4. Delete the `dcBlock1R.prepare(monoSpec); dcBlock1R.coefficients = dcCoeffs;` line.

5. Delete WR-01 dead variable at lines ~93-94:
       const float bypassRamp = static_cast<float>(samplesPerBlock) /
                                static_cast<float>(sampleRate * 0.01);
   Both lines go. Keep all four `bypassSmooth*.reset(sampleRate, 0.01)` calls and
   the four `setCurrentAndTargetValue(1.0f)` calls immediately below.

6. After the existing `dryBuffer.setSize(1, samplesPerBlock, false, true, false);`
   line, add three more pre-allocations:

       workBuffer      .setSize(2, samplesPerBlock, false, true, false);
       preUndBuffer    .setSize(2, samplesPerBlock, false, true, false);
       preBurninBuffer .setSize(2, samplesPerBlock, false, true, false);

   (Flag meanings: keepExistingContent=false, clearExtraSpace=true, avoidReallocating=false —
   standard pattern for one-time allocation with a known max size.)

In Source/PluginProcessor.cpp::releaseResources:

7. Delete `dcBlock1R.reset();` (only dcBlock1L exists now).

Build after these edits and confirm no warnings related to unused variables.
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -30 | grep -E "error|unused|dcCoeff|bypassRamp|dcBlock1R" || echo "BUILD OK"</automated>
  </verify>
  <done>
PluginProcessor.h has workBuffer, preUndBuffer, preBurninBuffer members and
dcBlock1R is gone. prepareToPlay sizes all four pre-allocated buffers. WR-01 and
WR-02 dead variables are deleted. releaseResources no longer references dcBlock1R.
Build is clean with no unused-variable warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite processBlock to use workBuffer for mono→stereo expansion (WR-04) and pre-allocated bypass snapshots; remove IN-01 dead call</name>
  <files>Source/PluginProcessor.cpp</files>
  <action>
Rewrite the body of `HeatDeathProcessor::processBlock` following the pipeline
diagram in the interfaces block above. The goal is to eliminate all allocation
and all host-buffer setSize calls. Keep every existing piece of logic intact
unless explicitly called out.

Specific edits — walk the current function top to bottom:

A. Keep step 0 (ScopedNoDenormals), step 1 (updateTimerState), step 2 (dryBuffer
   mono copy) exactly as they are.

B. LEAVE step 2/feedback injection code (the `if (pGlobalFeedbackActive->load() > 0.5f)`
   block at lines ~162-170) in place. Plan 02-04 owns the WR-03 fix.

C. LEAVE the input limiter AudioBlock/ProcessContextReplacing block exactly as is
   (lines ~176-181). It operates on buffer channel 0 which is mono at this point.

D. Stage 1 RAT section (lines ~184-216):
   - Keep bypassSmoothRat.setTargetValue line
   - Keep stageTurboRat->setParameters({...}) block
   - Keep stageTurboRat->process(buffer, numSamples)  — stage processes mono
     in-place on buffer channel 0
   - Keep the mono bypass crossfade loop (uses dryBuffer as dry source)
   - Keep `dcBlock1L.processSample` loop (mono)
   - Keep the post-RAT trim applyGain call — Plan 02-04 will inspect it but
     applyGain with a safe no-op trimGain stays correct

E. Stage 2 MicroPitch section — THIS IS THE WR-04 FIX. Replace the existing block
   (lines ~219-252) with:

       // Stage 2 — MicroPitch (mono in, stereo out).
       // WR-04 fix: mono→stereo expansion happens into the pre-allocated
       // workBuffer, not by resizing the host buffer.
       bypassSmoothPitch.setTargetValue(pPitchBypass->load() > 0.5f ? 0.0f : 1.0f);

       // Seed workBuffer channels 0 and 1 from the mono input (buffer ch 0).
       workBuffer.copyFrom(0, 0, buffer, 0, 0, numSamples);
       workBuffer.copyFrom(1, 0, buffer, 0, 0, numSamples);

       stageMicroPitch->setParameters ({
           .detuneL = pPitchDetuneL->load(),
           .detuneR = pPitchDetuneR->load(),
           .mix     = pPitchMix->load()   / 100.0f,
           .width   = pPitchWidth->load() / 100.0f
       });

       stageMicroPitch->process(workBuffer, numSamples);

       // Bypass crossfade (stereo) — mono dry is broadcast to both channels
       {
           auto* L = workBuffer.getWritePointer(0);
           auto* R = workBuffer.getWritePointer(1);
           const auto* dry = dryBuffer.getReadPointer(0);
           for (int i = 0; i < numSamples; ++i)
           {
               const float wet = bypassSmoothPitch.getNextValue();
               L[i] = L[i] * wet + dry[i] * (1.0f - wet);
               R[i] = R[i] * wet + dry[i] * (1.0f - wet);
           }
       }

       // DC block post-MicroPitch (stereo), operates on workBuffer
       {
           auto* L = workBuffer.getWritePointer(0);
           auto* R = workBuffer.getWritePointer(1);
           for (int i = 0; i < numSamples; ++i)
           {
               L[i] = dcBlock2L.processSample(L[i]);
               R[i] = dcBlock2R.processSample(R[i]);
           }
       }

       // Inter-stage trim post-MicroPitch — Plan 02-04 owns this, leave existing
       // applyGain but direct it at workBuffer instead of the host buffer.
       {
           const float trimGain = juce::Decibels::decibelsToGain(pTrimPostPitch->load());
           workBuffer.applyGain(trimGain);
       }

F. Stage 3 Undulator section — replace the inline stack allocation of preUndBuffer:

   Current (has IN-01 dead call and stack-ctor):
       stageMicroPitch->getLastLfoValue();    // IN-01 — delete
       ...
       juce::AudioBuffer<float> preUndBuffer (2, numSamples);   // DELETE — alloc!
       preUndBuffer.copyFrom(0, 0, buffer, 0, 0, numSamples);
       preUndBuffer.copyFrom(1, 0, buffer, 1, 0, numSamples);
       stageUndulator->process(buffer, numSamples);

   Replace with:
       // IN-01: Undulator cross-feed into MicroPitch detune will be wired
       // inside Undulator::process in Phase 5. The Phase 1 dead call on
       // stageMicroPitch->getLastLfoValue() has been removed.

       bypassSmoothUnd.setTargetValue(pUndBypass->load() > 0.5f ? 0.0f : 1.0f);

       stageUndulator->setParameters({ /* ...unchanged param struct... */ });

       // Snapshot pre-Undulator state for bypass crossfade, into pre-allocated member
       preUndBuffer.copyFrom(0, 0, workBuffer, 0, 0, numSamples);
       preUndBuffer.copyFrom(1, 0, workBuffer, 1, 0, numSamples);

       stageUndulator->process(workBuffer, numSamples);

       // Bypass crossfade (stereo) against preUndBuffer
       {
           auto* L = workBuffer.getWritePointer(0);
           auto* R = workBuffer.getWritePointer(1);
           const auto* dryL = preUndBuffer.getReadPointer(0);
           const auto* dryR = preUndBuffer.getReadPointer(1);
           for (int i = 0; i < numSamples; ++i)
           {
               const float wet = bypassSmoothUnd.getNextValue();
               L[i] = L[i] * wet + dryL[i] * (1.0f - wet);
               R[i] = R[i] * wet + dryR[i] * (1.0f - wet);
           }
       }

       // DC block post-Undulator (stereo) on workBuffer
       {
           auto* L = workBuffer.getWritePointer(0);
           auto* R = workBuffer.getWritePointer(1);
           for (int i = 0; i < numSamples; ++i)
           {
               L[i] = dcBlock3L.processSample(L[i]);
               R[i] = dcBlock3R.processSample(R[i]);
           }
       }

       // Trim post-Undulator on workBuffer (Plan 02-04 adjusts range)
       {
           const float trimGain = juce::Decibels::decibelsToGain(pTrimPostUnd->load());
           workBuffer.applyGain(trimGain);
       }

G. Stage 4 BurnIn section — replace inline stack allocation of preBurninBuffer:

   Current:
       juce::AudioBuffer<float> preBurninBuffer (2, numSamples);   // DELETE — alloc!
       ...
       stageBurnIn->process(buffer, numSamples);

   Replace with use of the pre-allocated member, processing into workBuffer:
       bypassSmoothBurnin.setTargetValue(pBurninBypass->load() > 0.5f ? 0.0f : 1.0f);

       preBurninBuffer.copyFrom(0, 0, workBuffer, 0, 0, numSamples);
       preBurninBuffer.copyFrom(1, 0, workBuffer, 1, 0, numSamples);

       stageBurnIn->setParameters({ /* ...unchanged param struct... */ });

       stageBurnIn->process(workBuffer, numSamples);

       // Bypass crossfade (stereo) against preBurninBuffer
       {
           auto* L = workBuffer.getWritePointer(0);
           auto* R = workBuffer.getWritePointer(1);
           const auto* dryL = preBurninBuffer.getReadPointer(0);
           const auto* dryR = preBurninBuffer.getReadPointer(1);
           for (int i = 0; i < numSamples; ++i)
           {
               const float wet = bypassSmoothBurnin.getNextValue();
               L[i] = L[i] * wet + dryL[i] * (1.0f - wet);
               R[i] = R[i] * wet + dryR[i] * (1.0f - wet);
           }
       }

H. NEW STEP — copy workBuffer back into the host `buffer`. The host buffer is
   declared stereo via the output bus layout (`withOutput("Output", stereo, true)`)
   so `buffer.getNumChannels()` is guaranteed to be 2 here. No setSize needed.

       // Copy fully processed stereo from workBuffer into host output buffer.
       buffer.copyFrom(0, 0, workBuffer, 0, 0, numSamples);
       buffer.copyFrom(1, 0, workBuffer, 1, 0, numSamples);

I. Keep step 8 (global wet/dry blend with mono dryBuffer) exactly as is — it
   operates on the host buffer after the workBuffer→buffer copy, so it blends
   the processed stereo against the mono dry just like before.

J. Keep step 9 (feedback update for next block) as is — Plan 02-04 rewrites it
   for WR-03.

K. Keep step 10 (output safety clip) as is.

After all these edits, the processBlock MUST:
   - Contain ZERO `new`, `malloc`, `std::vector`, `juce::AudioBuffer<float>` constructor,
     `buffer.setSize(...)`, or `workBuffer.setSize(...)` calls.
   - Contain NO `stageMicroPitch->getLastLfoValue();` dead call.
   - Contain NO reference to dcBlock1R.

Also in this task — apply the WR-05 fix in setStateInformation:

Replace lines ~506-512:
       persistedTemp     = static_cast<float> (
                               newState.getProperty ("persistedTemp",     0.0f));
       persistedTempPrev = static_cast<float> (
                               newState.getProperty ("persistedTempPrev", 0.0f));

       if (pBurninPersist->load() > 0.5f)
           stageBurnIn->setPersistedTemp (persistedTemp, persistedTempPrev);

With:
       const bool persistEnabled = (pBurninPersist->load() > 0.5f);
       persistedTemp     = persistEnabled
           ? static_cast<float>(newState.getProperty("persistedTemp",     0.0f))
           : 0.0f;
       persistedTempPrev = persistEnabled
           ? static_cast<float>(newState.getProperty("persistedTempPrev", 0.0f))
           : 0.0f;

       if (persistEnabled)
           stageBurnIn->setPersistedTemp(persistedTemp, persistedTempPrev);
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -30 | grep -E "error|warning" || echo "BUILD OK"</automated>
  </verify>
  <done>
processBlock is allocation-free and setSize-free. workBuffer is used for all
mono→stereo expansion and subsequent stereo processing. preUndBuffer and
preBurninBuffer are pre-allocated members used for bypass snapshots. IN-01 dead
call is removed with an explanatory comment. WR-05 fix is applied to
setStateInformation. Build is clean. A sine input produces a sine output through
the pass-through chain with no DC offset and no clicks when toggling any bypass.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these audits on Source/PluginProcessor.cpp:

1. No allocation inside processBlock:
     grep -nE "new |malloc|juce::AudioBuffer<float>\s*\w+\s*\(|\.setSize\s*\(" Source/PluginProcessor.cpp
   Every match must be outside processBlock (i.e. in prepareToPlay or
   releaseResources). Zero matches inside the processBlock function body.

2. No dead variables:
     grep -n "bypassRamp\|dcCoeff\|dcBlock1R\|getLastLfoValue" Source/PluginProcessor.cpp
   Should return ZERO results.

3. WR-05 fix:
     grep -n "persistEnabled" Source/PluginProcessor.cpp
   Should return matches inside setStateInformation.

4. Build:
     cmake --build build --config Release 2>&1 | tail -20
   No errors, no warnings.

5. Manual smoke test (if possible): load the plugin in a host, feed a 1kHz sine
   at -6 dBFS, confirm output is the same sine (pass-through stages) with no
   audible clicks when toggling each stage's bypass.
</verification>

<success_criteria>
- processBlock makes zero allocations and no host-buffer setSize calls.
- workBuffer, preUndBuffer, preBurninBuffer are pre-allocated stereo members.
- WR-01, WR-02, WR-04, WR-05 fixes all present.
- IN-01 dead call removed; IN-02 dcBlock1R removed.
- DC blocks fire correctly at three stage boundaries.
- Per-stage bypass crossfade still works click-free.
- Build green, sine test shows no DC accumulation.
- CHAIN-01, CHAIN-03, CHAIN-04, CHAIN-05 satisfied at the skeleton level.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure/02-03-SUMMARY.md`
documenting: each WR fix applied, allocation-free audit result, sine-test result.
</output>
</content>
</invoke>