---
phase: 02-core-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/utils/SmoothParam.h
autonomous: true
requirements:
  - PARAMS-04
  - CHAIN-05

must_haves:
  truths:
    - "SmoothParam::setTimeMs() computes a real one-pole IIR coefficient based on ms and sampleRate"
    - "SmoothParam::tick() advances the smoother per call (does not snap to target)"
    - "SmoothParam::reset(v) sets both current and target to v and clears any running ramp"
    - "A sine test through SmoothParam with abrupt target changes produces a continuous (not stepped) output"
  artifacts:
    - path: "Source/utils/SmoothParam.h"
      provides: "One-pole IIR smoother used by all DSP stages in Phases 3–6 for per-sample parameter smoothing"
      contains: "setTimeMs"
      min_lines: 30
  key_links:
    - from: "Source/utils/SmoothParam.h (tick)"
      to: "Source/utils/SmoothParam.h (coeff member)"
      via: "current = coeff * current + (1 - coeff) * target"
      pattern: "coeff\\s*\\*\\s*current"
---

<objective>
Replace the Phase 1 no-op `SmoothParam` stub with a working one-pole IIR smoother.
This utility is the smoothing primitive that DSP stage classes in Phases 3–6 will
use for per-sample parameter smoothing inside their own process loops. Phase 2-02
implements the math so the infrastructure is ready when stages are wired in later
phases.

Purpose: PARAMS-04 requires all float parameters smoothed over 20ms (and und_speed
over 50ms). The `juce::SmoothedValue<float>` instances inside PluginProcessor
(bypassSmooth*, globalMixSmooth, and the new stage-float smoothers added in Plan
02-03) handle smoothing at the processor boundary. `SmoothParam` is the stage-internal
analogue — each DSP stage (TurboRat, BurnIn, etc.) holds SmoothParam instances for
its internal parameters so per-sample coefficient updates can track knob motion
without zippering.

Output: A header-only, allocation-free one-pole smoother that matches the algorithm
contract documented in 02-RESEARCH.md section "SmoothParam Implementation".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-infrastructure/02-RESEARCH.md
@Source/utils/SmoothParam.h
@Source/dsp/TurboRat.h

<interfaces>
<!-- Current Source/utils/SmoothParam.h is a Phase 1 stub that snaps to target.
     Phase 2 replaces it with a real one-pole IIR while preserving the struct's
     external API so no caller code changes. -->

Existing (stub) API:
  struct SmoothParam {
      float current = 0.f;
      float target  = 0.f;
      void  setTimeMs(float ms, double sampleRate);
      void  setTarget(float v);
      float tick();
      void  reset(float v);
  };

This API must be preserved exactly. Only the internals of setTimeMs() and tick()
are replaced with real one-pole IIR math.

One-pole IIR LP formula (from RESEARCH.md):
  coeff = exp(-2π / (ms * sampleRate / 1000))
  tick: current = coeff * current + (1 - coeff) * target
  (coeff near 1.0 = slow smoother, coeff near 0.0 = fast smoother)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stub with one-pole IIR smoother</name>
  <files>Source/utils/SmoothParam.h</files>
  <behavior>
    - Contract 1 (compile): The existing four-method API (setTimeMs, setTarget,
      tick, reset) is preserved. Any file including SmoothParam.h continues to
      compile without edits.
    - Contract 2 (setTimeMs): Given `ms = 20.0f, sampleRate = 44100.0`, the
      computed coeff satisfies `0.0f < coeff < 1.0f`. Given `ms = 0.0f`, coeff
      evaluates to 0.0f (instantaneous), not NaN. Given `sampleRate = 0.0` the
      function must not divide by zero — treat sr <= 0 as a no-op (leave coeff
      at its previous value or 0.0f).
    - Contract 3 (reset): After `reset(0.5f)`, both current and target equal
      0.5f. A subsequent call to `tick()` returns 0.5f (no ramp).
    - Contract 4 (tick convergence): After `reset(0.0f)`, `setTimeMs(20.0f, 44100)`,
      and `setTarget(1.0f)`, 882 calls to `tick()` (== 20ms at 44.1kHz) advance
      `current` past 0.6 but below 1.0 (one time-constant ~= 63%). 10000 calls
      converge current above 0.999f.
    - Contract 5 (tick is strictly monotonic): When moving from 0.0 → 1.0 with
      a finite smoothing time, each successive tick() returns a strictly greater
      value than the previous (no snapping, no oscillation).
    - Contract 6 (no allocation, no dynamic dispatch, no heap): The struct remains
      a POD-style aggregate. Header-only. No virtual, no std::function, no
      std::unique_ptr, no external state.
  </behavior>
  <action>
Rewrite Source/utils/SmoothParam.h so it implements a one-pole IIR LP with the
following structure (header-only):

    #pragma once
    #include <cmath>
    #include <juce_core/juce_core.h>

    // Per-sample one-pole IIR smoother.
    // Used by DSP stages for per-sample parameter smoothing (Phases 3–6).
    // For smoothing of processor-level floats, prefer juce::SmoothedValue<float>.
    struct SmoothParam
    {
        float current = 0.0f;
        float target  = 0.0f;
        float coeff   = 0.0f;  // 0 = instant, approaches 1.0 = slow

        // Configure the smoothing time constant.
        // ms: time to reach ~63% of target (one time constant).
        // sampleRate: processor sample rate (Hz).
        // ms <= 0 or sampleRate <= 0 → instant (coeff = 0).
        void setTimeMs (float ms, double sampleRate) noexcept
        {
            if (ms <= 0.0f || sampleRate <= 0.0)
            {
                coeff = 0.0f;
                return;
            }
            const double samples = static_cast<double>(ms) * sampleRate / 1000.0;
            coeff = static_cast<float>(
                std::exp(-juce::MathConstants<double>::twoPi / samples));
        }

        void setTarget (float v) noexcept { target = v; }

        // Advance one sample. Returns the new current value.
        float tick() noexcept
        {
            current = coeff * current + (1.0f - coeff) * target;
            return current;
        }

        // Jump current and target to v. Clears any pending ramp.
        void reset (float v) noexcept
        {
            current = target = v;
        }
    };

Notes for the executor:
- Keep it header-only; do not create a .cpp file.
- `juce::MathConstants<double>::twoPi` requires the juce_core include — it is
  already pulled in transitively by JUCE headers in any translation unit that
  includes a DSP stage header, but include it explicitly in this file to be safe.
- Do NOT use `juce::SmoothedValue<float>` here — that class is used at the
  processor level (Plan 02-03 wires it into PluginProcessor for stage-float
  smoothing). SmoothParam is the stage-internal analogue and must stay a simple
  POD struct so stage classes can aggregate many of them cheaply.
- Mark tick/setTimeMs/setTarget/reset noexcept so the compiler can inline them
  aggressively into hot per-sample loops.

After the rewrite, confirm any existing callers still compile. At this stage the
only callers are DSP stage stubs that may include SmoothParam.h from TurboRat.h
or similar. Grep for `SmoothParam` across Source/ and confirm each use site still
compiles (no existing code relies on the stub's snap-to-target behaviour — all
current usage is uninitialised members, not tick() calls).
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -20 | grep -E "error|SmoothParam" || echo "BUILD OK"</automated>
  </verify>
  <done>
SmoothParam is a working one-pole IIR smoother. Build is clean.
setTimeMs(20ms, 44100Hz) produces a coeff in (0, 1). reset(v) snaps current and
target to v. tick() advances monotonically toward target. Header-only, allocation-
free, no virtual dispatch.
  </done>
</task>

</tasks>

<verification>
Spot-check the math by inspection:
  setTimeMs(20.0f, 44100.0):
    samples = 20 * 44100 / 1000 = 882
    coeff   = exp(-2π / 882) ≈ exp(-0.00712) ≈ 0.99290
  At this coeff, after 882 ticks with target=1.0 starting from current=0.0,
  current ≈ 0.632 (one time constant).

Build verification:
  cmake --build build --config Release 2>&1 | tail -10

Callers audit:
  grep -rn "SmoothParam" Source/ — every reference should still compile.
</verification>

<success_criteria>
- SmoothParam replaces the stub with a one-pole IIR.
- API surface unchanged (setTimeMs, setTarget, tick, reset).
- Build green, no new warnings.
- Smoother is allocation-free and header-only.
- Ready for use by stage classes in Phases 3–6.
- PARAMS-04 smoothing infrastructure (stage-side) is in place.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure/02-02-SUMMARY.md`
documenting: smoother formula used, callers verified compiling, build status.
</output>
</content>
</invoke>