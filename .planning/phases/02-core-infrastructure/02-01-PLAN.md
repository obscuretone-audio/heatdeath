---
phase: 02-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/Parameters.cpp
  - Source/Parameters.h
  - Source/PluginProcessor.cpp
  - Source/PluginProcessor.h
autonomous: true
requirements:
  - PARAMS-01
  - PARAMS-02
  - PARAMS-03
user_setup: []

must_haves:
  truths:
    - "All 50+ parameter IDs from Parameters.h resolve to live APVTS parameters at construction time"
    - "Every cached pointer in PluginProcessor (pRatDrive..pAcetateMode) is non-null after cacheParameterPointers()"
    - "DAW automation lanes display 6 groups: RAT / MicroPitch / Undulator / Burn-In / Trim / Global, plus Timer group and standalone Acetate Mode"
    - "No raw string ID lookups occur inside processBlock (all reads go through cached atomic<float>* pointers)"
  artifacts:
    - path: "Source/Parameters.cpp"
      provides: "createParameterLayout() with rat/pitch/und/burnin/trim/global/timer groups and acetate_mode"
      contains: "createParameterLayout"
    - path: "Source/PluginProcessor.cpp"
      provides: "cacheParameterPointers() resolving all atomic pointers with jassert sanity checks"
      contains: "cacheParameterPointers"
    - path: "Source/PluginProcessor.h"
      provides: "std::atomic<float>* member declarations for every Params:: ID"
      contains: "std::atomic<float>*"
  key_links:
    - from: "Source/PluginProcessor.cpp (constructor)"
      to: "cacheParameterPointers()"
      via: "direct call after APVTS construction"
      pattern: "cacheParameterPointers\\(\\)"
    - from: "Source/PluginProcessor.cpp (cacheParameterPointers)"
      to: "Source/Parameters.h (Params:: constants)"
      via: "using namespace Params; apvts.getRawParameterValue(ID)"
      pattern: "getRawParameterValue\\s*\\(\\s*[A-Z_]+"
    - from: "Source/PluginProcessor.cpp (processBlock)"
      to: "cached atomic pointers"
      via: "pXxx->load()"
      pattern: "p[A-Z][A-Za-z]+->load\\(\\)"
---

<objective>
Verify and lock down the APVTS parameter layout and pointer caching infrastructure.
The current Source/Parameters.cpp and Source/PluginProcessor.cpp already contain the
full implementation transplanted from `cpp files for agent/` during Phase 1. This
plan audits that implementation against Parameters.h, confirms every ID resolves,
and codifies a compile-time + runtime guard so future ID drift is caught immediately.

Purpose: PARAMS-01/02/03 require that all parameter IDs are defined centrally, all
pointers cached in the constructor, and all parameters grouped by hardware stage for
DAW automation lane display. Phase 1 put the plumbing in place; Phase 2-01 proves it
is complete, fixes any missing IDs/pointers, and leaves no gap between Parameters.h
and Parameters.cpp / PluginProcessor.cpp.

Output: A verified, auditable APVTS layout with a complete cached-pointer table and
runtime assertions that catch any typo on plugin load.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-infrastructure/02-RESEARCH.md
@.planning/phases/01-project-scaffold/01-REVIEW.md

@Source/Parameters.h
@Source/Parameters.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@cpp files for agent/Parameters.cpp
@cpp files for agent/PluginProcessor.h
@cpp files for agent/PluginProcessor.cpp

<interfaces>
<!-- Authoritative ID set — every one of these MUST appear in Parameters.cpp layout
     AND in PluginProcessor.h as a cached pointer AND in cacheParameterPointers(). -->

From Source/Parameters.h (Params namespace):

  RAT: RAT_DRIVE, RAT_FILTER, RAT_VOLUME, RAT_SLEW, RAT_ASYM,
       RAT_CLIP_MODE, RAT_SAG, RAT_BYPASS            (8 params)

  Pitch: PITCH_DETUNE_L, PITCH_DETUNE_R, PITCH_MIX, PITCH_WIDTH,
         PITCH_BYPASS                                (5 params)

  Undulator: UND_RATE, UND_DEPTH, UND_PHASE, UND_DRIFT, UND_MOD_RATE,
             UND_MOD_DEPTH, UND_MOD_SPEED, UND_SPREAD, UND_FEEDBACK,
             UND_GRIT, UND_MIX, UND_SHAPE, UND_BYPASS (13 params)

  BurnIn: BURNIN_AMOUNT, BURNIN_FREEZE, BURNIN_PERSIST, BURNIN_BYPASS (4 params)

  Trim: TRIM_POST_RAT, TRIM_POST_PITCH, TRIM_POST_UND (3 params)

  Global: GLOBAL_MIX, GLOBAL_FEEDBACK_AMOUNT, GLOBAL_FEEDBACK_ACTIVE,
          GLOBAL_MS_MODE                              (4 params)

  Timer: TIMER_DURATION, TIMER_ACTIVE                 (2 params)

  Standalone: ACETATE_MODE                            (1 param)

  TOTAL: 40 parameters

The APVTS layout in Parameters.cpp must use parameter Group objects with exactly
these names (display name, separator "|"):
  "rat"     / "RAT"
  "pitch"   / "MicroPitch"
  "und"     / "Undulator"
  "burnin"  / "Burn-In"
  "trim"    / "Inter-Stage Trims"
  "global"  / "Global"
  "timer"   / "Disintegration Timer"

`acetate_mode` is added to the layout as a standalone Bool (no group).

Every Params:: ID must appear exactly once in createParameterLayout(), and every
one must have a corresponding std::atomic<float>* member in PluginProcessor.h
and a resolve line in cacheParameterPointers() followed by a jassert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Parameters.cpp against Parameters.h — every ID present, grouped correctly</name>
  <files>Source/Parameters.cpp</files>
  <action>
Open Source/Parameters.cpp. Confirm `createParameterLayout()` constructs seven parameter
groups with these exact IDs and display names (per D-PARAMS-03 / RESEARCH.md Pattern 1):

  1. Group("rat", "RAT", "|", ...)
      — RAT_DRIVE  (Float, skewed 0..100 skew=0.7, default 72.0, label "%")
      — RAT_FILTER (Float, 0..100, default 50.0, label "%")
      — RAT_VOLUME (Float, 0..100, default 65.0, label "%")
      — RAT_SLEW   (Float, 0..100, default 68.0, label "%")
      — RAT_ASYM   (Float, 0..100, default 20.0, label "%")
      — RAT_CLIP_MODE (Choice, {"LED","Si","Lift","Ruetz"}, default 0)
      — RAT_SAG    (Float, 0..100, default 25.0, label "%")
      — RAT_BYPASS (Bool, default false)

  2. Group("pitch", "MicroPitch", "|", ...)
      — PITCH_DETUNE_L (Float, -25..-2, step 0.1, default -7.0, label "c")
      — PITCH_DETUNE_R (Float,  +2..+25, step 0.1, default +11.0, label "c")
      — PITCH_MIX      (Float, 0..100, default 85.0, label "%")
      — PITCH_WIDTH    (Float, 0..100, default 100.0, label "%")
      — PITCH_BYPASS   (Bool, default false)

  3. Group("und", "Undulator", "|", ...)
      — UND_RATE (Float, 0.5..8.5 Hz, skew 0.4, default 2.4, label "Hz")
      — UND_DEPTH (Float, 0..100, default 82.0, label "%")
      — UND_PHASE (Float, 0..360, step 1.0, default 180.0, label "deg")
      — UND_DRIFT (Float, 0..100, default 40.0, label "%")
      — UND_MOD_RATE  (Float, 0.1..2.1 Hz, skew 0.4, default 0.6, label "Hz")
      — UND_MOD_DEPTH (Float, 0..100, default 45.0, label "%")
      — UND_MOD_SPEED (Float, 0..100, default 35.0, label "%")
      — UND_SPREAD    (Float, 0..100, default 55.0, label "%")
      — UND_FEEDBACK  (Float, 0..100, skew 0.65, default 38.0, label "%")
      — UND_GRIT      (Float, 0..100, default 62.0, label "%")
      — UND_MIX       (Float, 0..100, default 100.0, label "%")
      — UND_SHAPE     (Choice, {"Sine","Triangle","Peak","Random","Envelope"}, default 0)
      — UND_BYPASS    (Bool, default false)

  4. Group("burnin", "Burn-In", "|", ...)
      — BURNIN_AMOUNT  (Float, 0..100, skew 0.6, default 35.0, label "%")
      — BURNIN_FREEZE  (Bool, default false)
      — BURNIN_PERSIST (Bool, default false)
      — BURNIN_BYPASS  (Bool, default false)

  5. Group("trim", "Inter-Stage Trims", "|", ...)
      — TRIM_POST_RAT   (Float, -12..+12 dB, step 0.1, default 0.0, label "dB")
      — TRIM_POST_PITCH (Float, -12..+12 dB, step 0.1, default 0.0, label "dB")
      — TRIM_POST_UND   (Float, -12..+12 dB, step 0.1, default 0.0, label "dB")

  6. Group("global", "Global", "|", ...)
      — GLOBAL_MIX              (Float, 0..100, default 100.0, label "%")
      — GLOBAL_FEEDBACK_AMOUNT  (Float, 0..100, skew 0.35, default 0.0, label "%")
      — GLOBAL_FEEDBACK_ACTIVE  (Bool, default false)
      — GLOBAL_MS_MODE          (Bool, default false)

  7. Group("timer", "Disintegration Timer", "|", ...)
      — TIMER_DURATION (Choice, {"10 min","20 min","40 min","74 min"}, default 2)
      — TIMER_ACTIVE   (Bool, default false)

  Standalone (added with layout.add(...) outside any group):
      — ACETATE_MODE (Bool, default false)

Each Float uses juce::AudioParameterFloat with juce::ParameterID{id, 1} (version stamp 1),
a NormalisableRange (linear or skewed per spec), default value, and an AudioParameterFloatAttributes
with .withLabel(...) for the UI unit. Use the existing `pid()`, `skewed()`, `Attr()`
shorthand aliases from the top of Parameters.cpp — do not inline juce:: types.

Every ID string MUST be referenced from Params:: — never write a raw string literal.
If any ID literal is found inline in Parameters.cpp, replace it with Params::<CONST>.

If any parameter is missing from the layout, ADD it. If any has the wrong range/default
relative to the spec in Parameters.h comments, FIX it. Do not remove any parameter.

After edits, confirm the layout compiles with no warnings and `layout` contains exactly
40 parameters distributed across 7 groups plus one standalone Bool.
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -20 | grep -E "error|warning" || echo "BUILD OK"</automated>
  </verify>
  <done>
Parameters.cpp compiles warning-free. Every Params:: ID from Parameters.h appears in
createParameterLayout() exactly once, in the correct group, with the correct
range/default/label. No raw string IDs present anywhere in Parameters.cpp.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit PluginProcessor.h atomic<float>* members and cacheParameterPointers() — every ID cached with jassert guard</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <action>
In Source/PluginProcessor.h, verify the public section declares one `std::atomic<float>*`
member for every Params:: ID listed in the interface block above. The existing file
already declares all 40 — confirm none are missing. Members must match this exact
naming convention (camelCase with p-prefix, matching Parameters.h ID roots):

  pRatDrive, pRatFilter, pRatVolume, pRatSlew, pRatAsym, pRatClipMode, pRatSag, pRatBypass
  pPitchDetuneL, pPitchDetuneR, pPitchMix, pPitchWidth, pPitchBypass
  pUndRate, pUndDepth, pUndPhase, pUndDrift, pUndModRate, pUndModDepth, pUndModSpeed,
  pUndSpread, pUndFeedback, pUndGrit, pUndMix, pUndShape, pUndBypass
  pBurninAmount, pBurninFreeze, pBurninPersist, pBurninBypass
  pTrimPostRat, pTrimPostPitch, pTrimPostUnd
  pGlobalMix, pGlobalFeedbackAmount, pGlobalFeedbackActive, pGlobalMsMode
  pTimerDuration, pTimerActive
  pAcetateMode

All default-initialised to nullptr.

In Source/PluginProcessor.cpp:

1. Confirm the constructor calls `cacheParameterPointers()` exactly once, AFTER APVTS
   has been constructed via the member initialiser list. Do not move the call to
   prepareToPlay() — APVTS storage is stable for the object lifetime per RESEARCH.md
   Pattern 2. (It is already in the correct location around line 30.)

2. In cacheParameterPointers(), confirm that every member pointer listed above has
   a matching line of the form:
       pXxx = apvts.getRawParameterValue(XXX);   // uses Params:: ID from `using namespace Params;`
   followed later by:
       jassert(pXxx != nullptr);

   Add any that are missing. If any jassert is missing for an existing cached pointer,
   add it. (The existing file should already have all 40 — audit and complete any gaps.)

3. Grep processBlock (and all other methods) for any occurrence of
   `apvts.getRawParameterValue(` or `apvts.getParameter(` — there must be ZERO such
   calls outside cacheParameterPointers(). If any are found, replace them with the
   cached pointer.

4. Grep processBlock for any raw string literal matching a Params:: ID (e.g. "rat_drive").
   There must be ZERO. If found, replace with the cached pointer read.

Leave all other processBlock logic intact for this task — Plans 02-03 and 02-04 will
handle the signal-chain fixes.
  </action>
  <verify>
    <automated>cmake --build build --config Release 2>&1 | tail -20 | grep -E "error" || echo "BUILD OK — no errors"</automated>
  </verify>
  <done>
All 40 Params:: IDs have a matching atomic<float>* member in PluginProcessor.h, a
resolve line in cacheParameterPointers(), and a jassert guard. No raw APVTS string
lookups exist outside cacheParameterPointers(). Build is clean.
  </done>
</task>

</tasks>

<verification>
Manual audit after task completion:

1. `grep -c "std::atomic<float>\*" Source/PluginProcessor.h` should return 40.
2. `grep -c "getRawParameterValue" Source/PluginProcessor.cpp` should return 40
   (all inside cacheParameterPointers()).
3. `grep -c "jassert.*!= nullptr" Source/PluginProcessor.cpp` should return ≥ 40.
4. `grep -n "getRawParameterValue" Source/PluginProcessor.cpp` — every match must be
   on a line between the `void HeatDeathProcessor::cacheParameterPointers()` opening
   brace and its closing brace.
5. `grep -nE '"(rat_|pitch_|und_|burnin_|trim_|global_|timer_|acetate_)' Source/PluginProcessor.cpp`
   should return ZERO results (no raw ID strings outside Parameters namespace).
6. Build: `cmake --build build --config Release` — no errors, no warnings on the
   modified files.
</verification>

<success_criteria>
- Parameters.cpp implements all 40 parameters in 7 groups + 1 standalone.
- PluginProcessor.h declares 40 std::atomic<float>* members.
- cacheParameterPointers() resolves and jasserts all 40.
- No raw-string APVTS lookups in processBlock or any other processor method.
- Build green. All jasserts pass on first plugin instantiation.
- PARAMS-01, PARAMS-02, PARAMS-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-infrastructure/02-01-SUMMARY.md`
documenting: parameter count confirmed, any missing IDs added, any gaps fixed,
build status.
</output>
</content>
</invoke>